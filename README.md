# MedTranslate

Doctor-patient language translation PWA for Indian hospitals.

## Development

1. Copy `.env.example` to `.env` and fill in your API keys
2. Run `npm install`
3. Run `npm run dev` — opens on http://localhost:3000

## Deployment

Deploy to Netlify. Set environment variables in Netlify dashboard.
